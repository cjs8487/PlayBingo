{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "required": ["goal", "description", "completedTeams"],
    "anyOf": [
        {"$ref": "#/$defs/RevealedCell"},
        {"$ref": "#/$defs/HiddenCell"}
    ],
    "$defs": {
        "RevealedCell": {
            "additionalProperties": false,
            "required": ["revealed", "goal", "completedTeams"],
            "properties": {
                "goal": {"$ref": "./Goal.json"},
                "completedTeams": {"type": "array", "items": {"type": "string"}},
                "revealed": { "enum": [ true ]}
            }
        },
        "HiddenCell": {
            "additionalProperties": false,
            "required": ["revealed", "completedTeams"],
            "properties": {
                "revealed": { "enum": [ false ]},
                "completedTeams": {"type": "array", "items": {"type": "string"}}
            }
        }
    }
}